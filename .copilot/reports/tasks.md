### Notionista - Tasks Summary

- Remove hard-coded Notion IDs from extension source and schemas — completed (placeholders used).
- Add migration and scanning scripts — completed (`scripts/*`).
- Implement safe onboarding (`notionista.init`) — completed and tested.
- Consolidate ESLint and TypeScript configs — completed.
- Add CI workflow — completed (`.github/workflows/ci.yml`).
- Prepare draft PR — template created.
